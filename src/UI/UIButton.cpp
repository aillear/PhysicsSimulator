# include "UIBotton.h"
# include "renderSystem.h"
# include "eventSystem.h"

void UIButton::draw() {
    // Draw the button based on its state
    if (isPressed) {
        
    }
    else if (isHovering) {

    }
    else {

    }
}